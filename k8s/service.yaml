apiVersion: v1
kind: Service
metadata:
  name: rag-api
  namespace: rag
spec:
  type: ClusterIP
  selector:
    app: rag-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
